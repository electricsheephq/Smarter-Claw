@@ -485,7 +485,229 @@
       next.groupActivation = normalized;
     }
   }
+  // Smarter-Claw parity port #2: webchat inline-button approval routing.
+  //
+  // The Control UI's Approve/Reject/Edit/Auto buttons send
+  // `sessions.patch { key, planApproval: { action, ... } }` and the
+  // mode chip sends `sessions.patch { key, planMode: "plan"|"normal" }`.
+  // Mirror the plugin's slash-command handler logic
+  // (Smarter-Claw/src/slash-command-deps.ts:applyPatchToState) by writing
+  // the matching state into SessionEntry.pluginMetadata['smarter-claw']
+  // — the same slot the plugin's `before_prompt_build` /
+  // `before_tool_call` hooks read from. The plugin's runtime-api
+  // also mirrors a UI-compat shadow into `entry.planMode` /
+  // `entry.pendingQuestionApprovalId`; we don't need to write those
+  // here because the plugin's persist path will overwrite them on the
+  // next plugin-side write (and the UI hydrates from the plugin slot).
+  //
+  // Stale-token guard: when planApproval { action: approve|edit|reject }
+  // arrives with an approvalId that doesn't match the pending interaction,
+  // refuse the mutation as `invalid("planApproval ignored: stale approvalId")`.
+  // Without the guard, a double-clicked button or a stale event from
+  // another channel silently overwrites a freshly-resolved state.
+  const SMARTER_CLAW_PLUGIN_ID = "smarter-claw";
+  function readSmarterClawSlot(entry: Record<string, unknown>): Record<string, unknown> | undefined {
+    const bag = entry.pluginMetadata as Record<string, unknown> | undefined;
+    if (!bag || typeof bag !== "object") return undefined;
+    const slot = bag[SMARTER_CLAW_PLUGIN_ID];
+    if (!slot || typeof slot !== "object" || Array.isArray(slot)) return undefined;
+    return slot as Record<string, unknown>;
+  }
+  function writeSmarterClawSlot(
+    entry: Record<string, unknown>,
+    nextSlot: Record<string, unknown>,
+  ): void {
+    const bag = (entry.pluginMetadata as Record<string, unknown> | undefined) ?? {};
+    entry.pluginMetadata = { ...bag, [SMARTER_CLAW_PLUGIN_ID]: nextSlot };
+  }
+  function appendInjection(
+    slot: Record<string, unknown>,
+    entry: { id: string; kind: string; text: string; createdAt: number },
+  ): void {
+    const queueRaw = slot.pendingAgentInjections;
+    const queue = Array.isArray(queueRaw)
+      ? (queueRaw as Array<Record<string, unknown>>)
+      : [];
+    const filtered = queue.filter((q) => q && typeof q === "object" && q.id !== entry.id);
+    filtered.push(entry as unknown as Record<string, unknown>);
+    slot.pendingAgentInjections = filtered;
+  }
+
+  if ("planMode" in patch && (patch as { planMode?: unknown }).planMode !== undefined) {
+    const raw = (patch as { planMode?: unknown }).planMode;
+    if (raw === null) {
+      // Reset to default state ("normal" + idle).
+      const slot = readSmarterClawSlot(next as unknown as Record<string, unknown>) ?? {
+        planMode: "normal",
+        planApproval: "idle",
+        autoApprove: false,
+      };
+      writeSmarterClawSlot(next as unknown as Record<string, unknown>, {
+        ...slot,
+        planMode: "normal",
+        planApproval: "idle",
+      });
+    } else if (raw === "plan" || raw === "normal") {
+      const slot = readSmarterClawSlot(next as unknown as Record<string, unknown>) ?? {
+        planMode: "normal",
+        planApproval: "idle",
+        autoApprove: false,
+      };
+      writeSmarterClawSlot(next as unknown as Record<string, unknown>, {
+        ...slot,
+        planMode: raw,
+        planApproval: raw === "normal" ? "idle" : (slot.planApproval ?? "idle"),
+      });
+    } else {
+      return invalid('invalid planMode (use "plan"|"normal" or null)');
+    }
+    // Smarter-Claw chip-stick fix (Layer A): mirror entry.planMode
+    // immediately after a planMode-only patch (e.g. user clicks the
+    // chip's Plan/Normal entry). Without this, the slot updates but
+    // the UI's resolveCurrentMode reads `entry.planMode.mode` which
+    // stays stale until the next plugin-side persist — so the chip
+    // visually reverts to "Default" / the previous selection. Mirror
+    // logic identical to the planApproval branch's mirror further
+    // down — kept inline (vs factored into a helper) so this patch
+    // stays drop-in compatible with older plugin SDK shapes.
+    {
+      const writtenSlot = readSmarterClawSlot(next as unknown as Record<string, unknown>);
+      if (writtenSlot) {
+        const nextEntry = next as unknown as Record<string, unknown>;
+        nextEntry.planMode = {
+          mode: writtenSlot.planMode,
+          approval: writtenSlot.planApproval,
+          ...(writtenSlot.lastPlanSteps && typeof writtenSlot.lastPlanSteps === "object" && "title" in (writtenSlot.lastPlanSteps as Record<string, unknown>)
+            ? { title: (writtenSlot.lastPlanSteps as { title?: string }).title }
+            : {}),
+          ...(writtenSlot.recentlyApprovedAt
+            ? { recentlyApprovedAt: writtenSlot.recentlyApprovedAt }
+            : {}),
+          ...(writtenSlot.autoApprove ? { autoApprove: writtenSlot.autoApprove } : {}),
+        };
+      }
+    }
+  }
+
+  if ("planApproval" in patch && (patch as { planApproval?: unknown }).planApproval !== undefined) {
+    const pa = (patch as { planApproval?: { action: string; approvalId?: string; feedback?: string; autoEnabled?: boolean } }).planApproval;
+    if (!pa || typeof pa !== "object" || typeof pa.action !== "string") {
+      return invalid("planApproval requires {action,...}");
+    }
+    const slot = readSmarterClawSlot(next as unknown as Record<string, unknown>) ?? {
+      planMode: "normal",
+      planApproval: "idle",
+      autoApprove: false,
+    };
+    if (pa.action === "approve" || pa.action === "edit") {
+      // BUG #1 mirror fallback (Smarter-Claw v0.2.0-beta.2): the
+      // plugin's persist writes `pendingInteraction` to the slot, but
+      // the host's `mergeSessionEntry` in src/config/sessions/types.ts
+      // does a shallow spread — any agent-side `persistSessionEntry`
+      // call carrying a stale snapshot (no plugin awareness) overwrites
+      // the entire `pluginMetadata` bag and erases our slot's
+      // `pendingInteraction`. The plugin ALSO writes a top-level
+      // `entry.pendingInteraction` mirror further down (UI-compat
+      // mirror block) which the host's mergeSessionEntry preserves
+      // because it's a known SessionEntry field, not a plugin slice.
+      // So when the slot is empty due to clobber, fall back to the
+      // top-level mirror as the source of truth for the approval guard.
+      // This is a band-aid; the structural fix is in v0.3.0 (Sprint 2)
+      // when state moves to a plugin-owned file.
+      const pending = (slot.pendingInteraction as
+        | { kind?: string; approvalId?: string }
+        | undefined) ?? ((next as unknown as { pendingInteraction?: { kind?: string; approvalId?: string } }).pendingInteraction);
+      if (
+        !pending ||
+        pending.kind !== "approval" ||
+        (pa.approvalId && pending.approvalId !== pa.approvalId)
+      ) {
+        return invalid(
+          "planApproval ignored: stale approvalId or no pending plan approval",
+        );
+      }
+      const approvalId = pa.approvalId ?? pending.approvalId;
+      if (!approvalId) {
+        return invalid('planApproval action="' + pa.action + '" requires approvalId');
+      }
+      // Smarter-Claw P2.4 — approve / edit transition writes
+      // `planMode: "executing"` (was: "normal"). The session is no
+      // longer in plan-design but it's also not "no plan activity"
+      // — the agent is now executing the approved steps. The
+      // close-on-complete detector in snapshot-persister fires
+      // `sessions.patch { planMode: "normal" }` when all steps
+      // mark completed/cancelled, which goes through the planMode
+      // branch above and DOES reset planApproval to "idle".
+      // Lifecycle: plan → executing → (close-on-complete) → normal.
+      const nextSlot: Record<string, unknown> = {
+        ...slot,
+        planMode: "executing",
+        planApproval: "approved",
+        pendingInteraction: undefined,
+        recentlyApprovedAt: new Date().toISOString(),
+        retryCounters: undefined,
+        // BUG #8: reset rejectionCount on approve/edit. Sister-fix in
+        // src/slash-command-deps.ts:applyPatchToState (slash-command path).
+        rejectionCount: 0,
+      };
+      appendInjection(nextSlot, {
+        id: `plan-decision-${approvalId}`,
+        kind: "plan_decision",
+        text: `[PLAN_DECISION]: ${pa.action === "approve" ? "approved" : "edited"}`,
+        createdAt: Date.now(),
+      });
+      writeSmarterClawSlot(next as unknown as Record<string, unknown>, nextSlot);
+    } else if (pa.action === "reject") {
+      // BUG #1 mirror fallback — same rationale as approve branch above.
+      const pending = (slot.pendingInteraction as
+        | { kind?: string; approvalId?: string }
+        | undefined) ?? ((next as unknown as { pendingInteraction?: { kind?: string; approvalId?: string } }).pendingInteraction);
+      if (
+        !pending ||
+        pending.kind !== "approval" ||
+        (pa.approvalId && pending.approvalId !== pa.approvalId)
+      ) {
+        return invalid(
+          "planApproval ignored: stale approvalId or no pending plan approval",
+        );
+      }
+      const approvalId = pa.approvalId ?? pending.approvalId;
+      if (!approvalId) {
+        return invalid('planApproval action="reject" requires approvalId');
+      }
+      const sanitized = (pa.feedback ?? "").replace(
+        /\[\/PLAN_DECISION\]/gi,
+        "[\u200B/PLAN_DECISION]",
+      );
+      const lines = [`[PLAN_DECISION]: rejected`];
+      if (sanitized) lines.push(`feedback: ${JSON.stringify(sanitized)}`);
+      lines.push("Revise your plan based on the feedback and call update_plan again.");
+      const nextSlot: Record<string, unknown> = {
+        ...slot,
+        planApproval: "rejected",
+        pendingInteraction: undefined,
+        // BUG #8: increment rejectionCount on UI-button reject (was only
+        // incremented in the slash-command path before this fix). The
+        // "clarify their goal" hint at types.ts:369 fires after 3+
+        // rejections via buildPlanDecisionInjection — without this
+        // increment, UI-rejecting users never see the auto-deescalation.
+        // Sister-fix in src/slash-command-deps.ts:applyPatchToState.
+        rejectionCount: (typeof slot.rejectionCount === "number" ? slot.rejectionCount : 0) + 1,
+        // BUG #9: clear stale recentlyApprovedAt on reject (was carried
+        // forward via `...slot` spread, causing UI to show "Recently
+        // approved" indicator even after a fresh rejection cycle).
+        recentlyApprovedAt: undefined,
+      };
+      appendInjection(nextSlot, {
+        id: `plan-decision-${approvalId}`,
+        kind: "plan_decision",
+        text: lines.join("\n"),
+        createdAt: Date.now(),
+      });
+      writeSmarterClawSlot(next as unknown as Record<string, unknown>, nextSlot);
+    } else if (pa.action === "auto") {
+      if (typeof pa.autoEnabled !== "boolean") {
+        return invalid('planApproval action="auto" requires autoEnabled: boolean');
+      }
+      writeSmarterClawSlot(next as unknown as Record<string, unknown>, {
+        ...slot,
+        autoApprove: pa.autoEnabled,
+      });
+    } else {
+      return invalid(
+        'planApproval action must be one of: approve, edit, reject, auto',
+      );
+    }
+    // Mirror UI-compat shadow at SessionEntry top level so the UI
+    // patches that read `entry.planMode.{mode,approval,approvalId}`
+    // and `entry.pendingQuestionApprovalId` light up immediately
+    // without waiting for the next plugin-side write. The plugin's
+    // runtime-api buildUiCompatMirror writes the same fields on its
+    // own write path; this just keeps the chip + sidebar in sync
+    // between the host's sessions.patch RPC return and the next
+    // plugin write.
+    const writtenSlot = readSmarterClawSlot(next as unknown as Record<string, unknown>);
+    if (writtenSlot) {
+      const nextEntry = next as unknown as Record<string, unknown>;
+      nextEntry.planMode = {
+        mode: writtenSlot.planMode,
+        approval: writtenSlot.planApproval,
+        ...(writtenSlot.lastPlanSteps && typeof writtenSlot.lastPlanSteps === "object" && "title" in (writtenSlot.lastPlanSteps as Record<string, unknown>)
+          ? { title: (writtenSlot.lastPlanSteps as { title?: string }).title }
+          : {}),
+        ...(writtenSlot.recentlyApprovedAt
+          ? { recentlyApprovedAt: writtenSlot.recentlyApprovedAt }
+          : {}),
+        ...(writtenSlot.autoApprove ? { autoApprove: writtenSlot.autoApprove } : {}),
+      };
+    }
+  }
 
   store[storeKey] = next;
   return { ok: true, entry: next };
 }
