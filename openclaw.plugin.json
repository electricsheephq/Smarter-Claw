{
  "id": "smarter-claw",
  "name": "Smarter-Claw",
  "description": "Plan-Mode plugin for OpenClaw v2026.6.1-beta.1. Plan-then-execute workflow with mutation gate, archetype prompting, escalating retry, rejection-cycle tracking, and task-flow visibility for pending approvals.",
  "minHostVersion": "2026.6.1-beta.1",
  "contracts": {
    "tools": [
      "enter_plan_mode",
      "exit_plan_mode",
      "ask_user_question"
    ],
    "sessionAttachments": [
      "active-session"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Globally enable or disable Smarter-Claw. When false, the plugin registers zero hooks (effectively uninstalled without manifest removal)."
      },
      "planTierModel": {
        "type": "string",
        "description": "Optional model override for plan-mode turns. Routed via the before_model_resolve hook. Examples: 'anthropic/claude-opus-4-7'. Requires allowConversationAccess. Leave empty to use the host's default model."
      },
      "planTierProvider": {
        "type": "string",
        "description": "Optional provider override paired with planTierModel. Use when the override model lives on a different provider than the session's default."
      },
      "plansBaseDir": {
        "type": "string",
        "description": "W1-F2: override the base directory under which exit_plan_mode persists the rendered plan markdown (<baseDir>/<agentId>/plans/plan-YYYY-MM-DD-<slug>.md). Defaults to ~/.openclaw/agents (in-host parity)."
      }
    }
  },
  "ui": {
    "configurationHints": {
      "enabled": {
        "label": "Enabled",
        "help": "Globally enable or disable Smarter-Claw plan-mode behavior."
      },
      "planTierModel": {
        "label": "Plan-Tier Model",
        "help": "Optional higher-tier model routed for plan-mode turns (e.g. 'anthropic/claude-opus-4-7'). Requires allowConversationAccess."
      },
      "planTierProvider": {
        "label": "Plan-Tier Provider",
        "help": "Optional provider override paired with Plan-Tier Model."
      },
      "plansBaseDir": {
        "label": "Plan Persistence Base Directory",
        "help": "Override the on-disk root for persisted plan markdown files. Defaults to ~/.openclaw/agents."
      }
    }
  },
  "documentation": {
    "requiredOperatorConfig": "Smarter-Claw's full plan-mode behavior (archetype injection, auto-continue, escalating retry) requires the conversation-access flag: `plugins.entries.smarter-claw.hooks.allowConversationAccess: true`. The mutation gate works without this flag. See the README's 'Required Operator Config' section.",
    "releaseTarget": "OpenClaw v2026.6.1-beta.1 (GitHub release tag v2026.6.1-beta.1, commit 2fc497e67b9cf40b2c12a9355afd785e7f8672dc). The matching npm package is not published; SDK fallback validation uses openclaw@2026.5.31-beta.4 plus release-tag seam audits.",
    "hostSeamGates": "Stock v2026.6.1-beta.1 still restricts active-session attachments to bundled plugins and does not include the chat-stream renderer seam from upstream PR #80982. Smarter-Claw keeps /plan slash commands, sidebar actions, persisted Markdown plan paths, and task-flow visibility as the releasable fallback."
  }
}
