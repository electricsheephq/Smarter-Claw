@@ -96,6 +96,7 @@ const LOCAL_COMMANDS = new Set([
   "kill",
   "steer",
   "redirect",
+  "plan",
 ]);
 
 const UI_ONLY_COMMANDS: SlashCommandDef[] = [
@@ -118,6 +119,16 @@ const UI_ONLY_COMMANDS: SlashCommandDef[] = [
     executeLocal: true,
     tier: "power",
   },
+  {
+    key: "plan",
+    name: "plan",
+    description: "Toggle plan mode (blocks write/edit/exec until approved)",
+    args: "<on|off|status|view|auto>",
+    icon: "book",
+    category: "session",
+    executeLocal: true,
+    argOptions: ["on", "off", "status", "view", "auto"],
+  },
 ];
 
 const CATEGORY_OVERRIDES: Partial<Record<string, SlashCommandCategory>> = {
@@ -141,6 +152,7 @@ const CATEGORY_OVERRIDES: Partial<Record<string, SlashCommandCategory>> = {
   compact: "session",
   focus: "session",
   unfocus: "session",
+  plan: "session",
   model: "model",
   models: "model",
   think: "model",
