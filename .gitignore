# Dependency directories (match both directories AND the LEXAR
# symlink we use to offload heavy installs off the root volume).
node_modules
node_modules/
.pnpm-store/

# Per-machine pnpm overrides (e.g. LEXAR store-dir on Eva's machine).
# See .npmrc for the template.
.npmrc.local

# Build output
dist/
*.tsbuildinfo

# Test artifacts
coverage/
.vitest/
.nyc_output/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Environment
.env
.env.*
!.env.example

# Log files
*.log
logs/
.npm/
.pnpm-debug.log*
.yarn/

# OpenClaw local install artifacts
.openclaw-install-stage-*/
.openclaw-install-backups/

# pnpm
pnpm-debug.log*
.claude/

# Audit work products (temporary artifacts from in-session audits)
AUDIT-*.md
